# Evidence fixtures

These files are bounded synthetic demo inputs. They are not historical
incidents and must not be presented as real provider reports. Each fixture
uses the approved `status.example.org` placeholder domain and a content hash
label so tests can swap in deterministic validator responses.
